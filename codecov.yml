codecov:
  require_ci_to_pass: false

coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

fixes:
  - "packages/core/llmtest_core/::packages/core/llmtest_core/"
